<head>
    <title>Edit Gossip N°<%= params["id"] %></title>
  </head>
  <body>
  <% if @gossip.errors.any? %>
<div class="alert alert-danger" role="alert">
<p>Nous n avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
<ul>
  <% @gossip.errors.full_messages.each do |message| %>
    <li><%= message %></li>
  <% end %>
</ul>
</div>
<% end %>
<div class="card-header text-center">
Edit Gossip N°<%= params["id"] %>
  </div>
   </br>
   <%= form_for @gossip do |f| %>
    <div class="d-flex justify-content-around text-center">
      <div class="card " style="width: 18rem;">
        <div class="card-body">
          <p class="card-text">Titre <%= f.text_field :title, class: 'form-control' %></p>
          <p class="card-text">Content <%= f.text_area :content , class: 'form-control' %>
          <hr class="my-4">
          <button type="submit", class="btn-primary" > Editer le gossip </button>
        </div>
      </div>
    </div>
  <% end %>
 
  </body>
</html>


 